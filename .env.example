# API Endpoints
# For development: Leave commented to use Vite proxy (configured in vite.config.js)
# For production: Nginx will handle API proxying
# VITE_YELLOW_CARD_API=/api
# VITE_ATTENDANCE_API=/api

# App Config
VITE_APP_TITLE=Global Dashboard
VITE_APP_VERSION=1.0.0

# NOTE: Do NOT use full URLs (e.g., https://attendance.anyemp.com/api)
# in development as it will bypass Vite proxy and cause CORS errors.
# Always use relative paths (/api) for development.
